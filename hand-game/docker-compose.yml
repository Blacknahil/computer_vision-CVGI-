version: '3.8'

services:
  detector:
    build:
      context: .
      dockerfile: Dockerfile.detector
    ports:
      - "8765:8765"
    # Note: Webcam access in docker on mac is hard. 
    # devices:
    #   - /dev/video0:/dev/video0

  game:
    build:
      context: ./client
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    volumes:
      - ./client:/app
      - /app/node_modules
    # depends_on:
    #   - detector
